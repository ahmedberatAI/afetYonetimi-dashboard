# Minimal dependencies for the Streamlit dashboard.
#
# Install (local):
#   pip install -r requirements_dashboard.txt

streamlit>=1.56
pandas
numpy
pydeck
plotly>=5.18
qrcode[pil]>=7.4
Pillow
streamlit-autorefresh>=1.0.1

# Tweet Test sekmesi (live single-text inference) icin gerekli.
# torch CPU wheel ~200 MB; Streamlit Community Cloud Linux runtime'inda
# `torch` + `transformers` bu dosyadan otomatik kurulur. Lokal'de GPU/CUDA
# isteyenler kendi wheel'larini kurabilir; varsayilan CPU build her ikisinde
# de calisir. Model checkpoint repo icinde tutulmaz: HuggingFace Hub'dan
# `from_pretrained("user/repo")` ile cekilir veya lokal `AFETYONETIMI_MODEL_DIR`
# uzerinden gosterilir.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.40
huggingface-hub
